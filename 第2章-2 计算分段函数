第2章-2 计算分段函数[1] (10 分)
本题目要求计算下列分段函数f(x)的值：

公式

输入格式:
输入在一行中给出实数x。

输出格式:
在一行中按“f(x) = result”的格式输出，其中x与result都保留一位小数。

def f(x):
    if x != 0:
        x_1 = 1 / x
    else:
        x_1 = 0
    x_1 = float("%.1f"% x_1)
    return x_1


x = int(input())
x_2 = float("%.1f"% x)
result = f(x_2)
print("f(%.1f) = "%x_2  + str(result))
